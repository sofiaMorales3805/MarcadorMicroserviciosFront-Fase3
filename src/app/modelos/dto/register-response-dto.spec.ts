import { RegisterResponseDto } from './register-response-dto';

describe('RegisterResponseDto', () => {
  it('should create an instance', () => {
    expect(new RegisterResponseDto()).toBeTruthy();
  });
});
